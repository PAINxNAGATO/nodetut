const num1 = 10;
const num2 = 5;
function add(a,b){
console.log(`the sum is ${a+b}`);
}
// const addvalues = (a,b) => {
//   console.log(`the sum is ${a+b}`);
// };
// addvalues(num1,num2);
add(num1,num2);
//if we do not export anything 
// like module.exports = add ;
// and still call the code the whole code will run in import 
// go to imkport.